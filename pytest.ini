[pytest]
pythonpath = host
testpaths = host
# ROS Jazzy puts pytest plugins on PYTHONPATH. They crash this project
# (missing lark, unknown hooks). Disable them by entry-point name.
addopts =
    -p no:launch_testing
    -p no:launch_ros
    -p no:launch
    -p no:ament_xmllint
    -p no:ament_flake8
    -p no:ament_lint
    -p no:ament_pep257
    -p no:ament_copyright
