# Agent 共通指示

このリポジトリで作業するエージェントは、以下に従う。

## レポートの出し方

調査・計画・実施の出力は `agent_reports/` に置く。ファイル名は連番 + 内容が分かる短い英語またはローマ字にする。

```
agent_reports/001_plan.md
agent_reports/002_phase0.md
```

次の番号は、既存の最大番号 + 1。同じ作業の追記・体裁直しは新規ファイルを増やさず、該当ファイルを更新する。

生ログ・バイナリ断片・JSON 要約は `agent_reports/<topic>_artifacts/` に分け、本文からパスで参照する。

## 本文の構成

すべてのレポートを、この順で書く。見出しを入れ替えない。

1. **背景** — この業務全体の中の位置づけと現状。計画のどの段か、本体とPCに何が入っているか。チャットの弁明を書かない。
2. **目的** — 背景に対して達成したい事柄。完了したら何が真になるか。表にしてよい。
3. **手段** — PC と StackChan に対するハード操作とソフト（コード・コマンド）。それでどの結果が得られるか。だからその手段を選んだ理由。「やらないこと」だけでは手段にしない。サンプルレートやビット幅などの数値・型は手段に書かず、詳細へ回す。
4. **結論** — 先に答えを書く。版数、パス、選定結果、未完了とその理由をここに含める。詳細を読まなくても判断できる長さにする。**必ず ASCII のブロック図を1つ以上置く。** 図の正本は下の「結論のブロック図」に従う。
5. **詳細** — 結果だけでなく、**どうやって調べたか**を書く。Step に分けるときは、各Stepも背景・目的・手段を同じ定義で書く。

本文は可能な限り箇条書きにする。1項目は1事実。長い段落にしない。見出し・再現コマンド・ASCII図・コードはそのまま。正本は `.cursor/rules/bullet-lists.mdc`。

読者は大学院の初心者を想定する。エージェントが内部で知っていることを省略せず、同じコマンドを打ち直せば再現できるようにする。定義の正本は `.cursor/rules/background-purpose-means.mdc`。

背景・目的・手段を書いたあと、**手段 → 目的 → 背景** の順で検算する。手段が目的を真にし、目的が背景の欠けを埋めるか。通らなければ背景から書き直す。検算の詳細は同ルールの「書いたあとの検算」に従う。

背景・目的・手段は、チャット履歴を知らない第三者が読んでも意味が通ること。主人公は PC、StackChan、利用者、プログラムであり、エージェントではない。同ルールの「読者と主人公」に従う。

## 結論のブロック図

外枠の箱は登場人物だけ（`PC`、`StackChan`、`スマホ`）。プロセス名・ポート名・ファームファイル名を箱のタイトルにしない。箱の中に、その登場人物の内部で何が動くかを書く。矢印は箱と箱のあいだだけ。経路とデータを矢印の横に書く。未実施は点線と `（未実施）`。使わなかった構成は別図。

よい例:

```
+---------------------------+        USB         +---------------------------+
| PC                        | -----------------> | StackChan                 |
|                           |  1秒ごと JSON      |                           |
|  中でやること             |  日付・時刻・曜日  |  中でやること             |
|    いまの日時を3行にする  |                    |    JSON を受け取る        |
|    曜日は英語か日本語     |                    |    液晶に3行描く          |
|                           | <----------------- |      2026/09/08           |
|                           |  描けた / 切れた   |      22:58:33             |
|                           |                    |      Tuesday              |
|                           |                    |    2.5秒来なければ        |
|                           |                    |      「未接続」           |
+---------------------------+                    +---------------------------+
```

やってはいけない例:

```
[clock_server.py] --USB /dev/ttyACM0--> [clock_ws ファーム] --描画--> [液晶]
```

プロセスとポートが登場人物になっている。液晶は StackChan の中の動きであり、別の箱にしない。

## 詳細の書き方

詳細は再現手順である。読み手が同じ順で打てば同じことが起きる。最小単位は **説明行 → コマンド** の繰り返し。ラベル `コマンド：` / `引数：` / `- 目的：` は使わない。正本は `.cursor/rules/reproduce-commands.mdc`。

````markdown
# PCで、本体がUSBデバイスとして見えるか確認するために、接続一覧を出す
lsusb
````

````markdown
# PCで、シリアルの実名と権限を見るために、デバイスノードを出す
ls -l /dev/ttyACM* /dev/ttyUSB* 2>/dev/null
````

説明行は `#` で始め、PC または StackChan、目的、何をする、を1行に書く。引数・フラグ・リダイレクトはコマンド行に含める。ログは **結果詳細** に置く。失敗も残す。長いログはファイルに分け、結果詳細にはそのコマンドが出した範囲を置く。未実行は `（未実行）`。

詳細の操作手順の見出しは **実践方法** である。「プログラム」とラベルしない。実践方法の次は **結果**（短い事実）、その次が **結果詳細**（ログ）。コマンドの直後にログを置かない。データを本体へ渡すときは、実践方法に送り方を書く。送り方は口（経路）、運ぶもの、運ぶ操作、着く場所である。「送らない」だけでは送り方にしない。運ぶ操作は、打ったコマンドそのものを書き、そのコマンドが何をするかを続ける。

Python を実行するときは `.py` ファイルを置き、コマンドは `python3 そのファイル` とする。中身には背景と意図のコメントを多めに書く。レポートからは `[パス](相対リンク)` でそのファイルへ張る。bash の heredoc（`python3 - <<'PY'`）に本文を埋め込まない。正本は `.cursor/rules/python-as-files.mdc`。

やってはいけないこと:

- 説明なしでコマンドだけ並べる
- `コマンド：` と `引数：` に分けて目的を遠くに置く
- 複数コマンドを1つの説明にまとめる
- 成功だけ残し、失敗したコマンドを消す
- Python を bash の heredoc や `python3 -c` に埋め込む
- `.py` のパスをバッククォートだけにして、ファイルへのリンクを張らない
- 詳細の操作手順を「プログラム」とラベルする
- 送り方を「送らない」だけの列にする
- 送り方の運ぶ操作を、打ったコマンドなしの要約語だけにする
- 実践方法のコマンドの直後にログを置いて、結果と結果詳細を分けない

## 言語と範囲

- 本文は日本語。コマンド、ファイルパス、ログ抜粋はそのまま書く。
- 秘密情報（Wi-Fi パスワード、API キー）はレポートに書かない。必要なことだけ「ローカル設定が必要」と書く。
- 計画と実施を混ぜない。実施報告の結論で、計画の完了条件のどれが未完了かを明示する。
