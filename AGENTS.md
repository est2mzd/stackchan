# Agent 共通指示

このリポジトリで作業するエージェントは、以下に従う。

## レポートの出し方

調査・計画・実施の出力は `agent_reports/` に置く。ファイル名は連番 + 内容が分かる短い英語またはローマ字にする。

```
agent_reports/001_plan.md
agent_reports/002_phase0.md
```

次の番号は、既存の最大番号 + 1。同じ作業の追記・体裁直しは新規ファイルを増やさず、該当ファイルを更新する。

生ログ・バイナリ断片・JSON 要約は `agent_reports/<topic>_artifacts/` に分け、本文からパスで参照する。

## 本文の構成

すべてのレポートを、この順で書く。見出しを入れ替えない。

1. **背景** — 何が起きていて、なぜこの作業が必要か。前提と参照（ユーザ指示、前のレポート、外部記事）を短く置く。
2. **目的** — この文書が答える問い、または完了条件。表にしてよい。
3. **結論** — 先に答えを書く。版数、パス、選定結果、未完了とその理由をここに含める。詳細を読まなくても判断できる長さにする。**必ず ASCII のブロック図を1つ以上置く。** 図の正本は下の「結論のブロック図」に従う。
4. **詳細** — 結果だけでなく、**どうやって調べたか**を書く。

読者は大学院の初心者を想定する。エージェントが内部で知っていることを省略せず、同じコマンドを打ち直せば再現できるようにする。

## 結論のブロック図

外枠の箱は登場人物だけ（`PC`、`StackChan`、`スマホ`）。プロセス名・ポート名・ファームファイル名を箱のタイトルにしない。箱の中に、その登場人物の内部で何が動くかを書く。矢印は箱と箱のあいだだけ。経路とデータを矢印の横に書く。未実施は点線と `（未実施）`。使わなかった構成は別図。

よい例:

```
+---------------------------+        USB         +---------------------------+
| PC                        | -----------------> | StackChan                 |
|                           |  1秒ごと JSON      |                           |
|  中でやること             |  日付・時刻・曜日  |  中でやること             |
|    いまの日時を3行にする  |                    |    JSON を受け取る        |
|    曜日は英語か日本語     |                    |    液晶に3行描く          |
|                           | <----------------- |      2026/09/08           |
|                           |  描けた / 切れた   |      22:58:33             |
|                           |                    |      Tuesday              |
|                           |                    |    2.5秒来なければ        |
|                           |                    |      「未接続」           |
+---------------------------+                    +---------------------------+
```

やってはいけない例:

```
[clock_server.py] --USB /dev/ttyACM0--> [clock_ws ファーム] --描画--> [液晶]
```

プロセスとポートが登場人物になっている。液晶は StackChan の中の動きであり、別の箱にしない。

## 詳細の書き方

詳細の最小単位は次の組である。ラベル名を変えない。

Markdown は空行がないと隣接行が1段落になる。`コマンド：` の次、`- 目的：` の次、`引数：` ブロックの次は必ず空行にする。

````markdown
コマンド： lsusb

- 目的： PC が本体を USB デバイスとして認識しているか確認する

引数： なし

結果：
```
Bus 003 Device 009: ID 303a:1001 Espressif USB JTAG/serial debug unit
```
````

引数があるときは箇条書きにする。フラグ・パス・`2>/dev/null`・パイプ・サブコマンドも書く。目的はコマンドの直下の `- 目的：` にだけ書く。引数欄に目的を混ぜない。

````markdown
コマンド： ls -l /dev/ttyACM* /dev/ttyUSB* 2>/dev/null

- 目的： シリアルの実名と権限を見る

引数：
- `-l` — 権限・所有者・リンク先を出す
- `/dev/ttyACM*` — ESP32-S3 内蔵 USB シリアルの名前
- `/dev/ttyUSB*` — USB-UART 変換チップの名前。無い機体もある
- `2>/dev/null` — グロブ 0 件のエラーメッセージを捨てる

結果：
```
crw-rw---- 1 root dialout 166, 0 Sep  8 22:01 /dev/ttyACM0
```
````

やってはいけないこと:

- コマンド・引数・結果以外のトップレベルラベルを足す。目的は `コマンド：` 直下の `- 目的：` だけ
- 結果を散文だけにして、出力ブロックを付けない
- 複数コマンドを1組にまとめる
- 成功だけ残し、失敗したコマンドを消す
- 引数を空欄のままにする（無いなら `引数： なし`）
- `- 目的：` を省略する
- 引数欄に目的を混ぜる

長いログはファイルに保存し、結果欄にはそのコマンドが出した範囲を置く。未実行は結果欄に `（未実行）` と書く。

## 言語と範囲

- 本文は日本語。コマンド、ファイルパス、ログ抜粋はそのまま書く。
- 秘密情報（Wi-Fi パスワード、API キー）はレポートに書かない。必要なことだけ「ローカル設定が必要」と書く。
- 計画と実施を混ぜない。実施報告の結論で、計画の完了条件のどれが未完了かを明示する。
